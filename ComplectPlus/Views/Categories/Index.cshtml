@model IEnumerable<ComplectPlus.Models.Category>

@{
    ViewData["Title"] = "Категории";
}

<h1>Категории</h1>

<p>
    <a asp-action="Create">Добавить</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryName)
            </td>
            <td>
                    <a asp-controller="Сomponent" asp-action="Index" asp-route-id="@item.CategoryId">Комплектующие</a> |
                    <a asp-action="Edit" asp-route-id="@item.CategoryId">Редактировать</a> |
                    <a asp-action="Details" asp-route-id="@item.CategoryId">Детали</a> |
                    <a asp-action="Delete" asp-route-id="@item.CategoryId">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
